= provide(:title, t('.title'))
- if @levels.empty?
  h1 = t('.empty')
- else
  h1 = t('.title')

  table
    thead
      tr
        th = t('.name')
        th
        th

    tbody
      - @levels.each do |level|
        tr
          td = level.name
          td = link_to t('links.edit'), edit_level_path(level)
          td = link_to t('links.delete'), level, data: {:confirm => 'Are you sure?'}, :method => :delete

br

= link_to t('links.new'), new_level_path
