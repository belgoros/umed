= provide(:title, t('.title'))
.row
  .small-12.columns
    h1 =t('.title')
    = simple_form_for @quiz, method: :put, url: wizard_path do |f|
      = f.error_notification

      .form-inputs
        = f.association :level, collection: @levels, prompt: true

      .form-actions
        = f.button :submit, t('links.next')
